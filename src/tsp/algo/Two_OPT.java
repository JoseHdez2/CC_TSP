package tsp.algo;

import tsp.typedef.NodeArray;

/** Refinement algorithm. Reorganizes tour that crosses over itself in a node. */ 
public abstract class Two_OPT {
    public static NodeArray twoOPT(NodeArray messyTour){
        NodeArray organizedTour = new NodeArray();
        
        
        
        return organizedTour;
    }
}
